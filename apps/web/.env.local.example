# Copy this file to .env.local and fill in your Supabase project values.
# If these are left empty, Karia automatically falls back to bundled demo
# listings so you can run the app with `npm run dev` without any backend.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
